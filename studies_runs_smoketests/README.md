# Study smoke tests

Disposable smoke-test run directories belong here, grouped by study. Every
smoke artifact is ignored. A smoke run must never share a directory, DMW
branch, or MongoDB collection with a full run.
