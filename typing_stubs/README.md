# DMW type stubs

These stubs are deliberately limited to the two public DMW imports used by
`run_lmstudio_backend.py`. They support static checking without replacing the
published DMW distribution used by the runtime backend.

Run the scoped check from the repository root:

```bash
.venv/bin/pyright
```
