# DMW type stubs

These stubs cover only the two public DMW imports used by
`entrypoints/lmstudio_backend.py`. They support static checking without
replacing the published DMW distribution used by the runtime backend.

Run the scoped check from the repository root:

```bash
.venv/bin/pyright
```
